apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: development
resources:
  - ../../base
  - namespace.yaml
  - dragonfly.yaml
patches:
  - target:
      kind: Deployment
      name: simple-app
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1